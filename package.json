{
    "scripts": {
        "docs:dev": "vitepress dev",
        "docs:build": "vitepress build",
        "docs:preview": "vitepress preview",
        "build": "vitepress build",
        "deploy": "npm run docs:build && echo anpr.sthaarun.com.np > .vitepress/dist/CNAME && gh-pages -d .vitepress/dist"
    },
    "devDependencies": {
        "gh-pages": "^6.3.0",
        "vitepress": "^1.6.4"
    }
}